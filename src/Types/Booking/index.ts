export type BookingData = {
  title: string;
  time: string;
  duration: number;
  rate: number;
  name: string;
  phone: number;
};

export type SeatData = {
  seatNumber: number;
  disable: boolean;
};
